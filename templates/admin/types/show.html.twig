{% extends 'base.html.twig' %}

{% block title %}Types
{% endblock %}

{% block body %}
	<section class="customContainer">
		<h3 class="mx-auto my-5 text-center h3 text-clip xs:my-6 md:my-7">Administration Type
			<br>
			Gestion =>
			{{type.type}}
			<br>
			id =>
			{{ type.id }}</h3>


		<a href="{{ path('app_admin_types_index') }}">
			<div class="text-center buttonSecondary">Retour à la liste</div>
		</a>

		<a href="{{ path('app_admin_types_edit', {'id': type.id}) }}">
			<div class="text-center buttonSecondary">Modifier</div>
		</a>


		{{ include('admin/types/_delete_form.html.twig') }}
	</section>
{% endblock %}
