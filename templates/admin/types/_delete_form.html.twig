<form method="post" action="{{ path('app_admin_types_delete', {'id': type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.id) }}">
    <button class="w-full buttonSecondary"><p class="text-red-500 uppercase">Delete</p></button>
</form>
